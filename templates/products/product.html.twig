{% extends 'base.html.twig' %}


{% block body %}
    <div>
        <h2>{{ product.name }}</h2>
        <p>{{ product.description }}</p>
        <p>Price: {{ product.price }}</p>
    </div>
{% endblock %}

